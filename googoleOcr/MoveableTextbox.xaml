<UserControl x:Class="googoleOcr.MoveableTextbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:googoleOcr"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!--<Thumb Canvas.Left="32" Canvas.Top="37" Height="70" Name="mark"  Width="70" Background="LightBlue"
                    DragCompleted="mark_DragCompleted"
                    DragStarted="mark_DragStarted"
                    DragDelta="mark_DragDelta"
                    >
        <Thumb.Template>
            <ControlTemplate TargetType="Thumb">
                <Grid>
                    <Rectangle x:Name="markRect" Stroke="Black" Fill="White" Width="50" Height="50" />
                    <TextBox Text="hoge" Width="30" Height="30" />
                </Grid>
            </ControlTemplate>
        </Thumb.Template>
    </Thumb>-->
</UserControl>
